# FastAPI and web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Machine Learning
# NOTE:
# - microsoft/VibeVoice-ASR requires the VibeVoice ASR runtime modules.
# - `vibevoice==0.0.1` from PyPI is outdated and does NOT include those ASR modules.
# - Use the pinned GitHub archive (no local Git install required for users).
vibevoice @ https://github.com/microsoft/VibeVoice/archive/1807b858d4f7dffdd286249a01616c243e488c9e.zip
transformers==4.51.3
accelerate==1.6.0
huggingface_hub>=0.30.0,<1.0.0
torch>=2.2.0
torchaudio>=2.2.0
safetensors>=0.4.2

# Audio processing
librosa>=0.10.1
soundfile>=0.12.1
pydub>=0.25.1

# Utilities
numpy>=1.26.3
tqdm>=4.66.0
# pydantic 2.9+ required for Python 3.13 wheels (pydantic-core Rust extension)
pydantic>=2.9.0
python-dotenv>=1.0.0

# Build tools (only needed for creating standalone .exe, not for dev)
# pyinstaller==6.3.0

# Development tools (optional)
# black>=24.1.1
# isort>=5.13.2
# mypy>=1.8.0
# pytest>=7.4.4
# pytest-asyncio>=0.23.3

# Optional extras:
# pyannote.audio>=3.3.2          # external diarization fallback
# bitsandbytes>=0.43.0           # INT8 quantization backend (CUDA)
