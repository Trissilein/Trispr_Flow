# FastAPI and web server
# Pin lower bound only — allows newer patch versions with Python 3.11-3.13 wheels
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Machine Learning
transformers>=4.51.3,<5.0.0
torch>=2.2.0
torchaudio>=2.2.0
accelerate>=0.26.1
huggingface_hub>=0.28.0
safetensors>=0.4.2

# Audio processing
librosa>=0.10.1
soundfile>=0.12.1
pydub>=0.25.1

# Utilities
numpy>=1.26.3
tqdm>=4.66.0
# pydantic 2.9+ required for Python 3.13 wheels (pydantic-core Rust extension)
pydantic>=2.9.0
python-dotenv>=1.0.0

# Build tools (only needed for creating standalone .exe, not for dev)
# pyinstaller==6.3.0

# Development tools (optional)
# black>=24.1.1
# isort>=5.13.2
# mypy>=1.8.0
# pytest>=7.4.4
# pytest-asyncio>=0.23.3

# Optional extras:
# pyannote.audio>=3.3.2          # external diarization fallback
# bitsandbytes>=0.43.0           # INT8 quantization backend (CUDA)
