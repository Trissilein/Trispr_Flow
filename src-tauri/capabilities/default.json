{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for primary and assistant presence windows",
  "windows": ["main", "assistant_presence"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
